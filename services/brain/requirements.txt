# Core service
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
websockets>=12.0

# Simulation
mujoco>=3.1
numpy>=1.26

# STT (primary + fallback)
faster-whisper>=1.0
openai-whisper>=20231117
soundfile>=0.12
librosa>=0.10

# NOTE (ROCm): install PyTorch ROCm wheels BEFORE this file, e.g.:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/rocm6.2
# vLLM runs as a separate process:
#   vllm serve Qwen/Qwen2.5-7B-Instruct --host 0.0.0.0 --port 8001
